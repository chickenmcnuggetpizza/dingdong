// Make the page slowly rotate
setInterval(() => {
  let angle = 0;
  setInterval(() => {
    angle += 0.1;
    document.body.style.transform = `rotate(${angle}deg)`;
    document.body.style.transition = 'transform 0.2s linear';
}, 100);
}, 1000)
